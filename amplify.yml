version: 1
frontend:
  phases:
    preBuild:
      commands:
        - mkdir -p web
    build:
      commands:
        - cp index.html web/index.html
  artifacts:
    baseDirectory: web
    files:
      - '**/*'
  cache:
    paths: []
